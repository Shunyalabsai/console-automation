name: Deploy Dashboard to GitHub Pages

on:
  # VM / local automation pushes dashboard commits under docs/** — deploy without running the test workflow in Actions
  push:
    branches:
      - main
    paths:
      # Dashboard data only — not operator docs (e.g. automation-server.md)
      - "docs/data/**"
      - "docs/history/**"
      - "docs/exports/**"
      - "docs/artifacts/**"
      - "docs/index.html"
  workflow_run:
    workflows: ["Run Tests & Update Dashboard"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'push' || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/upload-pages-artifact@v4
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
